version: "3.3"
services:
  zomboid:
    build: .
    volumes:
      - ./pzserver:/opt/pzserver
      - ./steam:/root/.steam
    ports:
      - 16261:16261
    command:
      - /bin/sh
      - -c
      - |
        /opt/pzserver/start-server.sh